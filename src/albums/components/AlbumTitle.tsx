import { Typography } from 'antd'

const { Title, Text } = Typography;

export const AlbumTitle = () => {
  return (
    <>
      <Title level={2}>Register Album 💽</Title>
      <Text type="secondary">Every album is a story — track by track, side by side</Text>
    </>
  )
}
